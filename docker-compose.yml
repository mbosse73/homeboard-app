services:
  homeboard:
    image: mcr.microsoft.com/playwright:v1.61.1-noble
    container_name: homeboard
    working_dir: /app
    # npm ci statt npm install: installiert exakt die Versionen aus package-lock.json. Sonst kann
    # playwright auf eine neuere Version wandern als der im Image mitgelieferte Chromium.
    command: ["sh", "-c", "npm ci --omit=dev --no-audit --no-fund && node server.js"]
    ports:
      - "3080:3000"
    volumes:
      - /mnt/user/appdata/homeboard:/app
    environment:
      - PORT=3000
      - HOMEBOARD_DOCKER=1
      - TZ=Europe/Berlin
    restart: unless-stopped
